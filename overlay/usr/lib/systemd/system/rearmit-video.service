# ReARM.it hardware optimization service
# Apply optimisations
# This service may block the boot process and reboot the system

[Unit]
Description=ReARM.it hardware optimization
Before=basic.target
After=sysinit.target local-fs.target armbian-hardware-monitor.target
DefaultDependencies=no

[Service]
Type=oneshot
ExecStart=/usr/lib/rearmit/rearmit-video start
RemainAfterExit=yes
TimeoutStartSec=2min

[Install]
WantedBy=basic.target
